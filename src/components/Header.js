import React from 'react';
import styled from 'styled-components';

const Header = styled.header`
  display: block;
  background: black;
`

export default () =>
  <Header>
    <h1>Akar Icons</h1>
  </Header>

